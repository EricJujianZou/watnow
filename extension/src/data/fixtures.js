// Replaced when the extension is packaged for release.
// The demo fixtures only exist for recordings, so the shipped extension
// carries no made-up student, courses or assignments.

export const STUDENT = { name: "", initials: "" };

export const TERM = "";

export const COURSES = [];

export const ITEMS = [];

export const DEMO_SCRIPT = {
  moves: [],
  reminder: { itemId: "" },
  scanDelays: [200],
};
